# Environment Configuration
NODE_ENV=development

# Server Configuration
PORT=3000
PUBLIC_DOMAIN=localhost:3000
API_URL=http://localhost:3000

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/nuro_api

# Redis Configuration
REDIS_HOST=localhost

# S3/MinIO Configuration (for audio files and images)
S3_ENDPOINT=localhost
S3_ACCESS_KEY_ID=minio
S3_SECRET_ACCESS_KEY=minio

# Lock Store Configuration (memory or redis)
LOCK_STORE=memory

# JWT Secret (generate a secure random string)
JWT_SECRET=your-super-secret-jwt-key-here